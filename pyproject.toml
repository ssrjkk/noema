[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "noema"
version = "1.2.0"
description = "Production-grade AI reasoning engine with 22 domain modules"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "Noema" }]

dependencies = [
    # Core
    "pydantic>=2.13.5",
    "pydantic-settings>=2.15.0",
    "pyyaml>=6.0.3",

    # Async / HTTP
    "aiohttp>=3.14.3",

    # Cache / queues
    "redis>=8.1.0",

    # API
    "fastapi>=0.100",
    "uvicorn[standard]>=0.52.4",

    # Data structures
    "networkx>=3.6.1",
    "numpy>=1.24",
    "scikit-learn>=1.9.0",

    # CLI
    "rich>=15.0.0",
    "typer>=0.27.1",

    # Structured logging
    "structlog>=23.0",

    # Metrics
    "prometheus-client>=0.26.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=7.1.0",
    "pytest-timeout>=2.4.0",
    "pytest-benchmark>=5.2.3",
    "hypothesis>=6.167.1",
    "httpx>=0.28.1",
    "fakeredis>=2.37.1",
    "ruff>=0.1",
    "mypy>=2.3.0",
    "pre-commit>=4.6.2",
]
db = [
    "sqlalchemy[asyncio]>=2.0.51",
    "asyncpg>=0.31.0",
    "alembic>=1.19.1",
]
full = [
    "chromadb>=1.5.9",
    "sentence-transformers>=2.0",
    "openai>=3.8.0,<4.0",
    "z3-solver>=4.13",
]
sentry = [
    "sentry-sdk>=2.68.0",
]
grpc = [
    "grpcio>=1.60",
    "grpcio-tools>=1.83.0",
    "protobuf>=7.36.1",
]
vault = [
    "hvac>=2.4.0",
]

[project.scripts]
noema = "noema.cli:app"

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = ["noema/grpc/noema_engine_pb2*"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "A", "C4", "SIM", "TCH"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"demo.py" = ["E402"]
"noema/cli/*.py" = ["B008"]  # typer uses function calls in defaults

[tool.ruff.lint.isort]
known-first-party = ["noema"]

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
check_untyped_defs = false
disallow_any_unimported = false
disallow_any_expr = false
disallow_any_decorated = false
disallow_any_explicit = false
disallow_any_generics = false
disallow_subclassing_any = false
disallow_untyped_calls = false
disallow_untyped_defs = false
disallow_untyped_decorators = false
ignore_missing_imports = true
implicit_reexport = true
no_implicit_optional = false
strict_equality = true
warn_redundant_casts = true
warn_unused_ignores = true
exclude = "noema[\\\\/]grpc[\\\\/]noema_engine_pb2(_grpc)?\\.pyi?$"

[[tool.mypy.overrides]]
module = "noema.grpc.noema_engine_pb2"
ignore_errors = true

[[tool.mypy.overrides]]
module = "noema.grpc.noema_engine_pb2_grpc"
ignore_errors = true

[tool.hatch.build.targets.wheel]
packages = ["noema"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
