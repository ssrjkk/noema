apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "noema.fullname" . }}-config
  labels:
    {{- include "noema.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}