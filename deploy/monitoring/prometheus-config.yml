global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - prometheus-rules.yml

scrape_configs:
  - job_name: noema
    static_configs:
      - targets: ["noema:8000"]
    metrics_path: /admin/metrics
    scrape_interval: 10s
